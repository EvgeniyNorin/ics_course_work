#include "time.h"

/*
    Fetch current value of real time and convert to date_time custom structure.
    SDCC doesn't allow return structures, only pointers to structures
*/

date_time* get_real_time() {    
    return;
}